package model.enums;

/**
 * Defines the three user roles in the Flight Reservation System.
 * Each role has different permissions and access to system features.
 */
public enum UserRole {
    CUSTOMER,
    FLIGHT_AGENT,
    ADMIN
}
